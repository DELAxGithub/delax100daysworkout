name: Auto Fix Bug with Approval

on:
  issues:
    types: [labeled]

permissions:
  contents: write
  issues: write
  pull-requests: write

concurrency:
  group: auto-fix-${{ github.repository }}-issue-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  # Cost estimation when auto-fix-candidate label is added
  estimate-cost:
    if: |
      github.event_name == 'issues' && 
      github.event.action == 'labeled' && 
      github.event.label.name == 'auto-fix-candidate'
    runs-on: ubuntu-latest
    
    steps:
      - name: Check for existing workflow
        id: check-duplicate
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Check if there's already a cost estimate comment
          EXISTING_COMMENT=$(gh api \
            repos/${{ github.repository }}/issues/${{ github.event.issue.number }}/comments \
            --jq '[.[] | select(.user.login == "github-actions[bot]" and (.body | contains("自動修正のコスト見積もり")))] | length')
          
          if [ "$EXISTING_COMMENT" -gt 0 ]; then
            echo "duplicate=true" >> $GITHUB_OUTPUT
            echo "既に処理済みです"
            exit 0
          else
            echo "duplicate=false" >> $GITHUB_OUTPUT
            echo "新規処理を開始します"
          fi
          
      - name: Checkout repository
        if: steps.check-duplicate.outputs.duplicate == 'false'
        uses: actions/checkout@v4
        
      - name: Setup Python
        if: steps.check-duplicate.outputs.duplicate == 'false'
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          
      - name: Install dependencies
        if: steps.check-duplicate.outputs.duplicate == 'false'
        run: |
          pip install pygithub
          pip install tiktoken
          
      - name: Estimate cost
        if: steps.check-duplicate.outputs.duplicate == 'false'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python scripts/estimate_cost.py \
            --issue-number ${{ github.event.issue.number }} \
            --repo ${{ github.repository }} \
            --model claude-opus-4-20250514